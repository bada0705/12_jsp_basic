1. 3
main method에 들어가는 인자 값은
문자열 배열이다.

2. 4
default는 따로 작성하는 문구가
아니다.

3. 1
String은 자료형이 아닌 클래스다.

4. 2
메소드는 명령문의 집합이고, 결과
를 내보내는 return문을 작성한다.

5. 2
메소드 오버로딩에 변수명은 고려하지
않는다. 메소드 시그니처에 의한 타입
의 개수, 타입의 순서, 타입의 형태를
고려한다.

6. 1) 같은
   2) 인덱스(index)
   3) 배열 요소(element)

7.  number/100*100;
    number - number%100;
해당 문제는 여러 형태의 정답이 존재할 것입니다.
